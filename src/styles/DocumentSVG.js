import styled from "styled-components";
import { ReactComponent as document } from "../assets/document_exists.svg";

const DocumentSVG = styled(document)`
  height: min(350px, 80vw);
  width: 90%;
`;

export default DocumentSVG;
